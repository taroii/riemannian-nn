# Scientific stack (only what the experiments import).
numpy>=1.26
matplotlib>=3.8

# Geometry: the curvature knob, manifold ops, Riemannian optimizers.
# geoopt's kappa-Stereographic model is the single-curvature sweep knob.
geoopt>=0.5.0

# ---------------------------------------------------------------------------
# PyTorch.
#
#   Laptop / CPU (default -- what `pip install -r requirements.txt` gives you):
#     torch>=2.2          # arm64 macOS wheel ships CPU + MPS
#
#   NVIDIA server: install the CUDA build explicitly first, e.g. for CUDA 12.1:
#       pip install torch --index-url https://download.pytorch.org/whl/cu121
#     then `pip install -r requirements.txt` for the rest.
# ---------------------------------------------------------------------------
torch>=2.2
